{
  "kind": "collectionType",
  "collectionName": "employee_roles",
  "info": {
    "name": "Employee role"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "name": {
      "type": "string"
    },
    "tasks": {
      "via": "executors",
      "collection": "task"
    },
    "rules": {
      "via": "roles",
      "collection": "rule"
    }
  }
}
